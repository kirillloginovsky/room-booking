services:
  - type: web
    name: room-assets-api
    runtime: node
    rootDir: backend
    buildCommand: npm install && npx prisma generate
    startCommand: node src/server.js
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: NODE_ENV
        value: production
